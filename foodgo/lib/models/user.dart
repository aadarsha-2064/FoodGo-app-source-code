// foodgo_app/lib/models/user.dart

// A simple model to represent a user in the FoodGo app.
class User {
  final String email;    // The user's email address
  final String username; // The user's chosen username

  // Constructor for the User model.
  User({required this.email, required this.username});
}